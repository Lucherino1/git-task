const str: string = "abobus";
console.log(str);

const firstCommit: string = "Sosantos";
console.log(firstCommit);

interface Sosi {
  name: string;
  age: number;
}

const user: Sosi = {
  name: "oleg",
  age: 42,
};

console.log(user);
